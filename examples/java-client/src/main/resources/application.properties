# Server port for Spring Boot (not used for gRPC client)
server.port=8080

# gRPC Server Configuration
grpc.server.host=localhost
grpc.server.port=50051

# Logging
logging.level.com.example.hegrpc=DEBUG
logging.level.io.grpc=INFO
